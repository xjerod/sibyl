# AI Memory History

This directory stores immutable summary records for v1.1 AI-memory gates.

Each file should use `sibyl-ai-memory-history-summary-v1`, point back to the
full citable artifact or external artifact manifest, and preserve the exact
gate result used as the previous-run baseline for later regression checks.
