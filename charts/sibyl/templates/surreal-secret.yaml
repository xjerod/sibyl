{{- if not .Values.backend.surreal.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sibyl.fullname" . }}-surreal
  labels:
    {{- include "sibyl.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if .Values.backend.surreal.password }}
  password: {{ .Values.backend.surreal.password | b64enc | quote }}
  {{- else }}
  # Auto-generate password if not provided
  password: {{ randAlphaNum 24 | b64enc | quote }}
  {{- end }}
{{- end }}
